<%= form_for(:picture,
			url: {action: 'save'},
			html: {multipart: true}) do |form| %>

			Title: <%= form.text_field ("title") %><br/>
			Upload the picture <%= form.file_field("uploaded_picture") %><br/>

			<%= submit_tag("Upload file") %>
			<% end %>